{% extends 'base.html.twig' %}

{% block title %}Mise à jour espèce{% endblock %}

{% block body %}
    <h1>Mise à jour espèce</h1>

    {{ include('species/_form.html.twig', {'button_label': 'Mettre à jour'}) }}

    <a href="{{ path('species_index') }}">Retour à la liste</a>

    {# {{ include('species/_delete_form.html.twig') }} #}
{% endblock %}
